{
  "name": "SpatialQuickSortCollider",
  "category": "engines",
  "parent": "Collider",
  "description": "Collider using quicksort along axes at each step, using Aabb bounds. \n\n Its performance is lower than that of InsertionSortCollider (see `Colliders' performance <https://yade-dem.org/wiki/Colliders_performace>`_), but the algorithm is simple enought to make it good for checking other collider's...",
  "attributes": [
    {
      "name": "avoidSelfInteractionMask",
      "type": "int",
      "default": "0",
      "description": "This mask is used to avoid the interactions inside a group of particles. To do so, the particles must have the exact same mask and that mask should have one bit in common with this"
    },
    {
      "name": "boundDispatcher",
      "type": "auto",
      "default": ""
    }
  ]
}