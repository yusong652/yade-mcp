{
  "name": "FlatGridCollider",
  "category": "engines",
  "parent": "Collider",
  "description": "Non-optimized grid collider, storing grid as dense flat array. Each body is assigned to (possibly multiple) cells, which are arranged in regular grid between *aabbMin* and *aabbMax*, with cell size *step* (same in all directions). Bodies outsize (*aabbMin*, *aabbMax*) are handled gracefully,...",
  "attributes": [
    {
      "name": "aabbMax",
      "type": "Vector3",
      "default": "Vector3(0,0,0)",
      "description": "Upper corner of grid (approximate, might be rouded up to *minStep*."
    },
    {
      "name": "aabbMin",
      "type": "Vector3",
      "default": "Vector3(0,0,0)",
      "description": "Lower corner of grid."
    },
    {
      "name": "avoidSelfInteractionMask",
      "type": "int",
      "default": "0",
      "description": "This mask is used to avoid the interactions inside a group of particles. To do so, the particles must have the exact same mask and that mask should have one bit in common with this"
    },
    {
      "name": "boundDispatcher",
      "type": "auto",
      "default": ""
    },
    {
      "name": "step",
      "type": "float",
      "default": "0.0",
      "description": "Step in the grid (cell size)"
    },
    {
      "name": "verletDist",
      "type": "float",
      "default": "0.0",
      "description": "Length by which enlarge space occupied by each particle; avoids running collision detection at every step."
    }
  ]
}