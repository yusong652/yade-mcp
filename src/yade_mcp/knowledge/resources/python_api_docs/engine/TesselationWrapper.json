{
  "name": "TesselationWrapper",
  "category": "engines",
  "parent": "GlobalEngine",
  "description": "Handle the triangulation of spheres in a scene, build tesselation on request, and give access to computed quantities (see also the dedicated section in user manual <MicroStressAndMicroStrain>). The calculation of microstrain is explained in [Catalano2014a]_ \n\nSee example usage in script...",
  "attributes": [
    {
      "name": "n_spheres",
      "type": "int",
      "default": "0",
      "description": "|ycomp|"
    },
    {
      "name": "far",
      "type": "float",
      "default": "10000.",
      "description": "Defines the radius of the large virtual spheres used to define nearly flat boundaries around the assembly. The radius will be the (scene's) bounding box size multiplied by 'far'. Higher values will minimize the error theoretically (since the infinite sphere really defines a plane), but it may increase numerical errors at some point. The default should give a resonable compromize."
    },
    {
      "name": "alphaCapsVol",
      "type": "float",
      "default": "0.",
      "description": "The volume of the packing as defined by the boundary alpha cap polygons"
    },
    {
      "name": "grad_u",
      "type": "Matrix3",
      "default": "Matrix3.Zero",
      "description": "The Displacement Gradient Tensor"
    },
    {
      "name": "dead",
      "type": "bool",
      "default": "false",
      "description": "If true, this engine will not run at all; can be used for making an engine temporarily deactivated and only resurrect it at a later point.",
      "inherited_from": "Engine"
    },
    {
      "name": "ompThreads",
      "type": "int",
      "default": "-1",
      "description": "Number of threads to be used in the engine. If ompThreads<0 (default), the number will be typically OMP_NUM_THREADS or the number N defined by 'yade -jN' (this behavior can depend on the engine though). This attribute will only affect engines whose code includes openMP parallel regions (e.g. InteractionLoop). This attribute is mostly useful for experiments or when combining ParallelEngine with engines that run parallel regions, resulting in nested OMP loops with different number of threads at each level.",
      "inherited_from": "Engine"
    },
    {
      "name": "label",
      "type": "str",
      "default": "",
      "description": "Textual label for this object; must be valid python identifier, you can refer to it directly from python.",
      "inherited_from": "Engine"
    },
    {
      "name": "execTime",
      "type": "int",
      "default": "0",
      "description": "Cumulative time in nanoseconds this Engine took to run (only used if O.timingEnabled == ``True``).",
      "no_save": true,
      "inherited_from": "Engine"
    },
    {
      "name": "execCount",
      "type": "int",
      "default": "0",
      "description": "Cumulative count this engine was run (only used if O.timingEnabled == ``True``).",
      "no_save": true,
      "inherited_from": "Engine"
    },
    {
      "name": "timingDeltas",
      "type": "NoneType",
      "default": "None",
      "description": "Detailed information about timing inside the Engine itself. Empty unless enabled in the source code and O.timingEnabled == ``True``.",
      "no_save": true,
      "inherited_from": "Engine"
    }
  ],
  "methods": [
    {
      "name": "addBoundingPlane",
      "description": "add a bounding plane (in fact a sphere with very large radius) bounding the spheres along the direction 'axis' (0,1,2), on the 'positive' or negative side."
    },
    {
      "name": "applyAlphaForces",
      "description": "set permanent forces based on stress using an alpha shape"
    },
    {
      "name": "applyAlphaVel",
      "description": "set velocities based on a velocity gradient tensor using an alpha shape"
    },
    {
      "name": "calcAlphaStress",
      "description": "get the Love-Weber average of the Cauchy stress on the polyhedral caps associated to boundary particles"
    },
    {
      "name": "calcVolPoroDef",
      "description": "Return a table with per-sphere computed quantities. Include deformations on the increment defined by states 0 and 1 if deformation=True (make sure to define states 0 and 1 consistently)."
    },
    {
      "name": "computeDeformations",
      "description": "compute per-particle deformation. Get it with TesselationWrapper::deformation (id,i,j)."
    },
    {
      "name": "computeVolumes",
      "description": "compute volumes of all Voronoi's cells."
    },
    {
      "name": "defToVtk",
      "description": "Write local deformations in vtk format from states 0 and 1."
    },
    {
      "name": "defToVtkFromPositions",
      "description": "Write local deformations in vtk format from positions files (one sphere per line, with x,y,z,rad separated by spaces)."
    },
    {
      "name": "defToVtkFromStates",
      "description": "Write local deformations in vtk format from state files (since the file format is very special, consider using defToVtkFromPositions if the input files were not generated by TesselationWrapper)."
    },
    {
      "name": "deformation",
      "description": "Get individual components of the particle deformation tensors"
    },
    {
      "name": "deformationTensor",
      "description": "Get particle deformation (tensor)"
    },
    {
      "name": "getAlphaCaps",
      "description": "Get the list of area vectors for the polyhedral caps associated to boundary particles ('extended' alpha-contour). If alpha is not specified or null the minimum alpha resulting in a unique connected domain is used. Taking a smaller 'shrinked' alpha for placing the virtual spheres moves the enveloppe outside the packing, It should be ~(alpha-refRad) typically."
    },
    {
      "name": "getAlphaFaces",
      "description": "Get the list of alpha faces for a given alpha. If alpha is not specified or null the minimum alpha resulting in a unique connected domain is used"
    },
    {
      "name": "getAlphaGraph",
      "description": "Get the list of area vectors for the polyhedral caps associated to boundary particles ('extended' alpha-contour). If alpha is not specified or null the minimum alpha resulting in a unique connected domain is used"
    },
    {
      "name": "getAlphaVertices",
      "description": "Get the list of 'alpha' bounding spheres for a given alpha. If alpha is not specified or null the minimum alpha resulting in a unique connected domain is used. This function is generating a new alpha shape for each call, not to be used intensively."
    },
    {
      "name": "getVolPoroDef",
      "description": "|ydeprecated| see new name TesselationWrapper.calcVolPoroDef"
    },
    {
      "name": "loadState",
      "description": "Load a file with positions to define state 0 or 1."
    },
    {
      "name": "saveState",
      "description": "Save a file with positions, can be later reloaded in order to define state 0 or 1."
    },
    {
      "name": "setState",
      "description": "Make the current state of the simulation the initial (0) or final (1) configuration for the definition of displacement increments, use only state=0 if you just want to get  volmumes and porosity. Exclude bodies using the bitmask TesselationWrapper::groupMask."
    },
    {
      "name": "testAlphaShape",
      "description": "transitory function, testing AlphaShape feature"
    },
    {
      "name": "triangulate",
      "description": "triangulate spheres of the packing"
    },
    {
      "name": "volume",
      "description": "Returns the volume of Voronoi's cell of a sphere."
    }
  ]
}