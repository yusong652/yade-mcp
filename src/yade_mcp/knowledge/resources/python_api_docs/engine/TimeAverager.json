{
  "name": "TimeAverager",
  "category": "engines",
  "parent": "PartialEngine",
  "description": "Average data over time for specific sphere identified by ids. Data are position, velocity, angular velocity, global resultant force and torque, resultant force and torque computed from contacts only and contact force field (see description below). The data must be first initialized with its...",
  "attributes": [
    {
      "name": "computeContactForceField",
      "type": "bool",
      "default": "False",
      "description": "Wether to compute and average contact force field at the surface of the particles (experimental feature). The contact force field is obtained by ditributing the contact forces on a grid at the surface of the sphere. The contact forces are spread on each point of the grid, according to the distance between the contact point and the grid point. The algorythm uses a gaussian kernel to smooth the field. If computeContactForceField is true, grid and sigma parameters must be filled in. This can significantly increase the computation time for dense grid or high number of particles."
    },
    {
      "name": "grid",
      "type": "list",
      "default": "[]",
      "description": "Grid on which to compute the contact force field. Should be a list of 3D coordinates at the surface of the particle (a simple way to generate a well distributed grid at the surface of a sphere is with the Fibonacci lattice method)."
    },
    {
      "name": "ids",
      "type": "list",
      "default": "[]"
    },
    {
      "name": "sigma",
      "type": "float",
      "default": "0.0",
      "description": "Standard deviation of the Gaussian function, which determines how the contact forces are weighted based on their distance from a contact point. It is usually set at the order of the distance between two points in the grid."
    }
  ],
  "methods": [
    {
      "name": "getAngVel",
      "description": "Get averaged angular velocity of particle since last initialization"
    },
    {
      "name": "getContactForce",
      "description": "Get averaged resultant force computed from contact forces on particle since last initialization"
    },
    {
      "name": "getContactForceField",
      "description": "Get averaged contact force field at the surface of the particle since last initialization"
    },
    {
      "name": "getContactTorque",
      "description": "Get averaged resultant torque computed from contact forces on particle since last initialization"
    },
    {
      "name": "getForce",
      "description": "Get averaged resultant force of particle since last initialization"
    },
    {
      "name": "getNbContact",
      "description": "Get averaged number of contact points on particle since last initialization"
    },
    {
      "name": "getPos",
      "description": "Get averaged position of particle since last initialization"
    },
    {
      "name": "getTorque",
      "description": "Get averaged resultant torque of particle since last initialization"
    },
    {
      "name": "getVel",
      "description": "Get averaged velocity of particle since last initialization"
    },
    {
      "name": "initialization",
      "description": "Initialize tAccu to zero and the averaged variables to there instantaneous values. Necessary to execute before any simulation run, otherwise it crashes."
    }
  ]
}