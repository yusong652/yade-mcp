{
  "name": "HelixEngine",
  "category": "engines",
  "parent": "RotationEngine",
  "description": "Engine applying both rotation and translation, along the same axis, whence the name HelixEngine",
  "attributes": [
    {
      "name": "linearVelocity",
      "type": "float",
      "default": "0",
      "description": "Linear velocity [m/s]"
    },
    {
      "name": "angleTurned",
      "type": "float",
      "default": "0",
      "description": "How much have we turned so far. |yupdate| [rad]",
      "read_only": true
    },
    {
      "name": "angularVelocity",
      "type": "float",
      "default": "0",
      "description": "Angular velocity. [rad/s]",
      "inherited_from": "RotationEngine"
    },
    {
      "name": "rotationAxis",
      "type": "Vector3",
      "default": "Vector3r::UnitX()",
      "description": "Axis of rotation (direction); will be normalized automatically.",
      "inherited_from": "RotationEngine"
    },
    {
      "name": "rotateAroundZero",
      "type": "bool",
      "default": "false",
      "description": "If True, bodies will not rotate around their centroids, but rather around ``zeroPoint``.",
      "inherited_from": "RotationEngine"
    },
    {
      "name": "zeroPoint",
      "type": "Vector3",
      "default": "(0, 0, 0)",
      "description": "Point around which bodies will rotate if ``rotateAroundZero`` is True",
      "inherited_from": "RotationEngine"
    },
    {
      "name": "ids",
      "type": "list[int]",
      "default": "",
      "description": "Ids list of bodies affected by this PartialEngine.",
      "inherited_from": "PartialEngine"
    },
    {
      "name": "dead",
      "type": "bool",
      "default": "false",
      "description": "If true, this engine will not run at all; can be used for making an engine temporarily deactivated and only resurrect it at a later point.",
      "inherited_from": "Engine"
    },
    {
      "name": "ompThreads",
      "type": "int",
      "default": "-1",
      "description": "Number of threads to be used in the engine. If ompThreads<0 (default), the number will be typically OMP_NUM_THREADS or the number N defined by 'yade -jN' (this behavior can depend on the engine though). This attribute will only affect engines whose code includes openMP parallel regions (e.g. InteractionLoop). This attribute is mostly useful for experiments or when combining ParallelEngine with engines that run parallel regions, resulting in nested OMP loops with different number of threads at each level.",
      "inherited_from": "Engine"
    },
    {
      "name": "label",
      "type": "str",
      "default": "",
      "description": "Textual label for this object; must be valid python identifier, you can refer to it directly from python.",
      "inherited_from": "Engine"
    },
    {
      "name": "execTime",
      "type": "int",
      "default": "0",
      "description": "Cumulative time in nanoseconds this Engine took to run (only used if O.timingEnabled == ``True``).",
      "no_save": true,
      "inherited_from": "Engine"
    },
    {
      "name": "execCount",
      "type": "int",
      "default": "0",
      "description": "Cumulative count this engine was run (only used if O.timingEnabled == ``True``).",
      "no_save": true,
      "inherited_from": "Engine"
    },
    {
      "name": "timingDeltas",
      "type": "NoneType",
      "default": "None",
      "description": "Detailed information about timing inside the Engine itself. Empty unless enabled in the source code and O.timingEnabled == ``True``.",
      "no_save": true,
      "inherited_from": "Engine"
    }
  ]
}