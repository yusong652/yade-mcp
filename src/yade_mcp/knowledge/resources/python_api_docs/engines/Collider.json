{
  "name": "Collider",
  "category": "engines",
  "parent": "GlobalEngine",
  "description": "Abstract class for finding spatial collisions between bodies. \n\n.. admonition:: Special constructor\n\n\tDerived colliders (unless they override ``pyHandleCustomCtorArgs``) can be given list of BoundFunctors <BoundFunctor> which is used to initialize the internal boundDispatcher...",
  "attributes": [
    {
      "name": "avoidSelfInteractionMask",
      "type": "int",
      "default": "0",
      "description": "This mask is used to avoid the interactions inside a group of particles. To do so, the particles must have the exact same mask and that mask should have one bit in common with this"
    }
  ]
}