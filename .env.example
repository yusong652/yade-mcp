# Copy to .env and edit for local overrides. .env is gitignored.

# Optional external project directory mounted at /proj inside the dev container.
# Example (Windows host path, forward slashes):
# PROJ_MOUNT_SRC=G:/Han/proj_multidirection_yade
